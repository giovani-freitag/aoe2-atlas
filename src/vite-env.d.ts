/// <reference types="vite/client" />

/** Stamped in by Vite from package.json, so the running build can name itself. */
declare const __APP_VERSION__: string;
declare const __APP_REPOSITORY__: string;
